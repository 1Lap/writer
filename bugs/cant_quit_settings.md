After opening settings, the settings window won't go away. tried:

x in top right of window
cancel button
save button

none close the settings window, and using the taskbar icon "quit" does not quit.
