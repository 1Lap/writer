name: Build Release Executable

on:
  push:
    tags:
      - 'v*'  # Trigger on version tags like v1.0.0
  pull_request:
    branches:
      - main  # Build on PRs to main branch
  workflow_dispatch:  # Allow manual trigger

permissions:
  contents: write  # Required to create releases and upload assets
  pull-requests: write  # Required to comment on PRs

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-dev.txt
        pip install -r requirements-windows.txt

    - name: Run tests
      run: |
        pytest -v

    - name: Build executable with PyInstaller
      run: |
        pyinstaller --onedir `
          --name "LMU_Telemetry_Logger" `
          --icon=NONE `
          --add-data "src;src" `
          --hidden-import psutil `
          --hidden-import datetime `
          --hidden-import pystray `
          --hidden-import PIL `
          --collect-all src `
          tray_app.py

    - name: Install Inno Setup
      run: |
        choco install innosetup -y

    - name: Build installer with Inno Setup
      shell: pwsh
      run: |
        # Find Inno Setup compiler
        $isccPath = "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
        if (!(Test-Path $isccPath)) {
          Write-Error "Inno Setup compiler not found at: $isccPath"
          exit 1
        }

        Write-Output "Found Inno Setup at: $isccPath"

        # Run Inno Setup compiler
        & $isccPath installer\LMU_Telemetry_Logger_Setup.iss

        if ($LASTEXITCODE -ne 0) {
          Write-Error "Inno Setup compilation failed with exit code: $LASTEXITCODE"
          exit 1
        }

        # Verify installer was created
        if (!(Test-Path "installer\output\LMU_Telemetry_Logger_Setup_v1.0.0.exe")) {
          Write-Error "Installer was not created!"
          exit 1
        }

        Write-Output "Installer created successfully!"

    - name: Create release package
      shell: pwsh
      run: |
        # Create release directory
        New-Item -ItemType Directory -Force -Path release

        # Copy the installer (primary distribution method)
        Copy-Item "installer\output\LMU_Telemetry_Logger_Setup_v1.0.0.exe" "release\"

        # Create a zip of the standalone executable bundle for advanced users
        Compress-Archive -Path "dist\LMU_Telemetry_Logger\*" -DestinationPath "release\LMU_Telemetry_Logger_Standalone.zip" -Force

        # Copy documentation
        Copy-Item "README.md" "release\"
        Copy-Item "USER_GUIDE.md" "release\"
        Copy-Item "CHANGELOG.md" "release\"
        Copy-Item "WINDOWS_BUILD_INSTRUCTIONS.md" "release\"

        # Generate version info based on trigger type
        if ("${{ github.event_name }}" -eq "pull_request") {
          $version = "LMU Telemetry Logger PR #${{ github.event.pull_request.number }} ($(git rev-parse --short HEAD))`nBuild from: ${{ github.head_ref }}"
        } else {
          $version = "LMU Telemetry Logger ${{ github.ref_name }}"
        }
        Set-Content -Path "release\VERSION.txt" -Value $version

        Write-Output "Release package created successfully!"

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: LMU_Telemetry_Logger-windows-${{ github.event_name == 'pull_request' && format('PR{0}', github.event.pull_request.number) || github.ref_name }}
        path: release/

    - name: Comment on PR with build artifact
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          const artifactName = `LMU_Telemetry_Logger-windows-PR${{ github.event.pull_request.number }}`;
          const runId = context.runId;
          const repo = context.repo;

          const comment = `## ✅ Windows Build Complete

          The Windows executable and installer have been built and are ready for testing!

          ### Download
          📦 [Download **${artifactName}**](https://github.com/${repo.owner}/${repo.repo}/actions/runs/${runId})

          Click the link above, scroll down to "Artifacts" section, and download the build.

          ### What's included
          - \`LMU_Telemetry_Logger_Setup_v1.0.0.exe\` - **Windows installer (recommended)**
          - \`LMU_Telemetry_Logger_Standalone.zip\` - Standalone executable bundle (for advanced users)
          - \`README.md\`, \`USER_GUIDE.md\`, \`CHANGELOG.md\`
          - \`VERSION.txt\` - Build information

          ### Testing checklist
          **Installer:**
          - [ ] Run installer on Windows
          - [ ] Verify Start Menu shortcuts created
          - [ ] Test custom output directory selection
          - [ ] Test upgrade scenario (install v1.0.0, then newer version)
          - [ ] Test uninstall with data preservation options

          **Functionality:**
          - [ ] Complete a player lap - verify no \`mCurrentET\` errors
          - [ ] Join multiplayer session
          - [ ] Wait for opponent to complete a lap
          - [ ] Check opponent lap filename has opponent's name
          - [ ] Check opponent CSV metadata has correct driver/car

          ---
          🤖 *Automated build from PR #${{ github.event.pull_request.number }}*`;

          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: repo.owner,
            repo: repo.repo,
            body: comment
          });

    - name: Get release notes
      id: release_notes
      if: startsWith(github.ref, 'refs/tags/')
      shell: bash
      run: |
        # Extract version from tag (e.g., v0.2.0 -> 0.2.0)
        VERSION=${GITHUB_REF_NAME#v}

        # Look for RELEASE_NOTES_v{version}.md
        RELEASE_NOTES_FILE="RELEASE_NOTES_v${VERSION}.md"

        if [ -f "$RELEASE_NOTES_FILE" ]; then
          echo "Using $RELEASE_NOTES_FILE"
          echo "body_file=$RELEASE_NOTES_FILE" >> $GITHUB_OUTPUT
        else
          echo "No release notes file found, using default"
          echo "body_file=" >> $GITHUB_OUTPUT
        fi

    - name: Create GitHub Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v2
      with:
        files: |
          installer/output/LMU_Telemetry_Logger_Setup_v1.0.0.exe
          release/LMU_Telemetry_Logger_Standalone.zip
          README.md
          USER_GUIDE.md
          CHANGELOG.md
          WINDOWS_BUILD_INSTRUCTIONS.md
        body_path: ${{ steps.release_notes.outputs.body_file }}
        body: |
          ## LMU Telemetry Logger ${{ github.ref_name }}

          ### Features
          - ✅ Automatic telemetry capture from Le Mans Ultimate (player + opponents)
          - ✅ System tray interface with start/stop/pause controls
          - ✅ Professional Windows installer with shortcuts
          - ✅ Opponent lap tracking (fastest lap only)
          - ✅ MVP format output (12-channel LMUTelemetry v2)
          - ✅ ~43Hz capture rate (optimal)
          - ✅ File size: ~1 MB per lap
          - ✅ Smart file naming with track, car, driver, lap time

          ### Installation (Recommended)

          **Option 1: Installer (Easy)**
          1. Download `LMU_Telemetry_Logger_Setup_v1.0.0.exe`
          2. Run the installer and follow the wizard
          3. Choose your telemetry output directory (default: `Documents\LMU Telemetry`)
          4. Launch from Start Menu
          5. Access system tray icon for controls

          **Option 2: Standalone Executable (Advanced)**
          1. Download `LMU_Telemetry_Logger_Standalone.zip`
          2. Extract the zip file to a folder of your choice
          3. Run `LMU_Telemetry_Logger.exe` from the extracted folder
          4. CSV files saved to `./telemetry_output/`

          ### Prerequisites
          1. **Install LMU Runtime Dependencies** (Required!)
            - Navigate to: `LMU/support/runtimes/`
            - Install all runtime installers (especially `vc_redist.x64.exe`)
            - Restart computer (recommended)
          2. **Enable rF2SharedMemoryMapPlugin** in LMU
            - Edit `Documents\Studio 397\UserData\<YourPlayer>\CustomPluginVariables.JSON`
            - Set `"Enabled": 1` for `rF2SharedMemoryMapPlugin`
            - This plugin is included with LMU (just needs to be enabled)

          ### Requirements
          - Windows 10/11 (64-bit)
          - Le Mans Ultimate installed
          - **Visual C++ Runtimes** (from LMU's support/runtimes folder)

          ### What's New
          - 🎉 Professional Windows installer
          - 🎉 System tray UI with icon state indicators
          - 🎉 Start Menu and Desktop shortcuts
          - 🎉 Configurable output directory
          - 🎉 Auto-upgrade with data preservation

          ### Documentation
          - See `USER_GUIDE.md` for detailed instructions
          - See `CHANGELOG.md` for full changelog
          - See `WINDOWS_BUILD_INSTRUCTIONS.md` to build from source
          - See `installer/README.md` for installer build instructions
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
